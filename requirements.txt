# TradingAgents stock-analysis skill — Python dependencies
# Pin versions to match the verified working set; the skill was last
# validated against this lock on 2026-06-07.

# Core TradingAgents dependencies
langchain-core>=1.0,<2.0
langgraph>=1.0,<2.0
langgraph-checkpoint>=4.0
langgraph-checkpoint-sqlite>=3.0
langchain-openai>=1.0,<2.0

# Data flow utilities
pandas>=2.0
numpy>=1.24
pydantic>=2.0

# HTTP clients (used by ServiceHub Tushare proxy and LLM client)
httpx>=0.27
requests>=2.30

# A-share data (used as fallback inside TradingAgents China dataflows)
yfinance>=0.2
stockstats>=0.6
tushare>=1.4
akshare>=1.13

# Stock technical indicators
ta>=0.11

# Environment loading (.env is optional but the package itself supports it)
python-dotenv>=1.0
